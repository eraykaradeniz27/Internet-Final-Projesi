@model IEnumerable<projeödev.Models.OgrenciDers>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<a href="@Url.Action("OgrenciDersAdd","Home")" class="btn px-15 btn-primary">
    <i class="las la-plus fs-16"></i>Yeni Ders Atama
</a>
<div class="row mt">
    <div class="col-md-12">
        <div class="content-panel">
            <table class="table table-striped table-advance table-hover">
                <thead>
                    <tr class="userDatatable-header">
                        <th>
                            <span class="userDatatable-title">Id</span>
                        </th>
                        <th>
                            <span class="userDatatable-title float-right">Ders Adı</span>
                        </th>
                        <th>
                            <span class="userDatatable-title float-right">Öğrenci</span>
                        </th>
                        <th>
                            <span class="userDatatable-title float-right">Action</span>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Count() > 0)
                    {
                        foreach (var ogrenciDers in Model)
                        {
                            <tr>
                                <td>
                                    <div class="userDatatable-content" style="align-items:center">
                                        @ogrenciDers.OgrenciDersId
                                    </div>
                                </td>
                                <td>
                                    <div class="userDatatable-content">
                                        @ogrenciDers.Ders.DersAdi
                                    </div>
                                </td>
                                <td>
                                    <div class="userDatatable-content">
                                        @ogrenciDers.Ogrenci.AdSoyad
                                    </div>
                                </td>
                                <td>
                                    <ul class="orderDatatable_actions mb-0 d-flex flex-wrap">
                                        <li>
                                            <a href="@Url.Action("OgrenciDersEdit","Home", new {  id=ogrenciDers.OgrenciDersId  })" class="btn btn-primary btn-xs">
                                                <i class="fa fa-pencil"></i>
                                            </a>
                                        </li>
                                        <li>
                                            <a href="@Url.Action("OgrenciDersRemove","Home", new {  id=ogrenciDers.OgrenciDersId  })"class="btn btn-danger btn-xs">
                                                <i class="fa fa-trash-o "></i>
                                            </a>
                                        </li>
                                    </ul>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="3">&nbsp;</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
</div>

